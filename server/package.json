{
  "name": "api-server",
  "version": "1.0.0",
  "main": "api.js",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-paginate": "^1.0.2",
    "jsonwebtoken": "^8.5.1",
    "sequelize": "^6.3.5",
    "sequelize-cli": "^6.2.0",
    "sqlite3": "^5.0.0"
  },
  "scripts": {
    "db:undo:all": "./node_modules/.bin/sequelize db:migrate:undo:all",
    "db:migrate": "./node_modules/.bin/sequelize db:migrate",
    "db:seed:all": "./node_modules/.bin/sequelize db:seed:all",
    "db:init": "yarn db:undo:all && yarn db:migrate && yarn db:seed:all",
    "dev": "yarn db:init && ./node_modules/.bin/nodemon api.js"
  },
  "devDependencies": {
    "nodemon": "^2.0.5"
  }
}
